import { describe, it } from "vitest"

describe("habits actions", () => {
  it.todo("should fail to create habit without authentication")
  it.todo("should create a new habit")
  it.todo("should increment streak when completing habit")
  it.todo("should reset streak to 0 after missing a day")
  it.todo("should not increment streak if already completed today")
  it.todo("should delete a habit")
})
